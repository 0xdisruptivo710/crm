# Dependências e builds
node_modules/
dist/
.next/
.turbo/
coverage/

# Segredos — NUNCA commitar
.env
.env.*
!.env.example
# Explícito para qualquer subpasta (apps/web etc.) — reforça o .env.* acima.
**/.env.local

# Sistema
desktop.ini
.DS_Store

# Workspace do processo SDD (scratch)
.superpowers/
