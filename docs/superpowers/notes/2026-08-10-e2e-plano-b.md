# E2E do critério de pronto do Plano B — evidências (2026-08-10)

Registro do Task 12 (deploy + E2E final) — evidências do critério de pronto ponta-a-ponta descrito em `docs/superpowers/plans/2026-08-08-plano-b-pipeline-evolution.md` §"Critério de pronto do Plano B", com a API já deployada no EasyPanel via imagem GHCR.

## 1. Mensagem real → Customer/Conversation/Message/Timeline

Mensagem real de WhatsApp recebida pelo número do piloto (instância Evolution dev `aios-pocket`), processada pelo pipeline inbound (Task 10):

- **Customer** resolvido/criado com telefone canônico em **E.164**.
- **Conversation** resolvida/criada (`companyId + provider + conversationExternalId`).
- **Message** inserida com `direction=inbound`, progredindo pela máquina de estados **received → read** conforme os acks subsequentes da Evolution chegaram como webhooks separados (`MessageStatusUpdate`).
- **TimelineEvent** (`customer_events`) registrado para a chegada da mensagem, consultável e coerente com a Message e a Conversation.

## 2. Envio via API → chega no celular

- Envio feito via `POST /messages`, autenticado com **JWT REAL do Supabase** — não um mock: o caminho de verificação `apps/api/src/auth/verify.ts` validou o token contra o **JWKS de produção** do projeto Supabase (`createRemoteJWKSet`), provando o fluxo de auth ponta-a-ponta com credencial real, não a fixture mockada usada nos testes automatizados.
- Rota respondeu **202** imediatamente (Message criada `queued`, worker `message-send` processou depois — rota nunca esperou o provider, regra do CLAUDE.md §4).
- Mensagem **delivered no celular do piloto**, confirmando o ciclo completo API → fila → `EvolutionProvider.sendText` → WhatsApp real.
- `providerMessageId` (pmid) do envio real: **`3EB0D75F9A2E796890EBAF`**.

## 3. Resposta do celular → outbound sem duplicata

- O piloto respondeu **do próprio celular** (não pela API) à conversa.
- O webhook `fromMe` correspondente foi processado pelo pipeline inbound e virou uma **Message outbound** (não inbound) — a guarda `fromMe` do Domain.md/ADR-0003 aplicada corretamente: o histórico não mentiu, a mensagem entrou como resposta do humano, não como se fosse do cliente.
- **Sem duplicata**: a guarda de eco (`process-webhook.ts`, achado da re-review T10/T11) reconheceu que não havia conflito com nenhum envio prévio pela API para este pmid, e criou exatamente uma linha.

## Tráfego real observado

Com o webhook definitivo apontando para a API deployada no EasyPanel, o tráfego real da instância dev fluiu de forma estável durante a janela de verificação:

- **52 `raw_webhook_events`** arquivados / **31 `domain events`** publicados em uma janela de **15 minutos** de operação contínua — proporção esperada dado que múltiplos raws (ex.: acks intermediários) não geram necessariamente um evento de domínio novo cada.

## Produção intocada

Confirmado ao final: instância `murilo`, webhook do n8n e projeto `aios` do EasyPanel seguem exatamente como estavam antes do Plano B — nenhuma mudança na produção real do usuário.

## Credenciais

**Todas as credenciais que circularam durante o Plano B (Postgres dev, Redis dev, API key da Evolution dev) foram ROTACIONADAS em 2026-08-10, imediatamente após a revisão final da branch.** Os valores antigos (inclusive os que apareceram em versões do plano e em histórico de conversa) estão mortos e não devem ser reutilizados; ver também a nota equivalente no topo de `docs/superpowers/plans/2026-08-08-plano-b-pipeline-evolution.md`.
